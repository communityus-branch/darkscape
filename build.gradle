allprojects {
    apply plugin: 'idea'
}

subprojects {
    apply plugin: 'java'

    group 'rs.darkscape'
    version '1.0-SNAPSHOT'

    sourceCompatibility = 1.8

    repositories {
        mavenCentral()
    }
}
