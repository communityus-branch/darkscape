syntax = "proto3";

import "options.proto";
import "packet.proto";

option java_package = "rs.darkscape.proto";

message ArchiveServiceConnect {
  option (darkscape.packet.mapping).id = 15;
  option (darkscape.packet.mapping).type = INBOUND;

  uint32 revision = 1 [(darkscape.packet.field).type = INT32];
}