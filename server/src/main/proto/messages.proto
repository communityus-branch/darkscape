syntax = "proto3";

import "mappings.proto";

option java_package = "rs.darkscape.proto";

message ArchiveServiceConnect {
  option (darkscape.packet).id = 15;
  option (darkscape.packet).source = CLIENT;

  uint32 revision = 1 [(darkscape.field).type = INT32];
}

message Test {

}