syntax = "proto3";

import "google/protobuf/descriptor.proto";

option java_package = "rs.darkscape.proto";

enum Length {
  STATIC = 0;
  BYTE = 1;
  SHORT = 2;
}

enum Type {
  INBOUND = 0;
  OUTBOUND = 1;
}

enum FieldType {
  INT8 = 0;
  INT16 = 1;
  INT32 = 2;
  INT64 = 3;
  STRING = 4;
}